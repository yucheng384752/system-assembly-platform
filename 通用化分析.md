# 通用化分析

> 適用專案：Form System Kit Composer 及準備納入 Composer 的業務系統  
> 更新日期：2026-08-11  
> 依據：先前「通用化討論」與「找出通用化缺口」對話紀錄；本版依漏洞審查修訂，並依 Composer 平台（SaaS）與交付物雙信任邊界補充第14節

## 1. 目的與完成定義

通用化的目標，是讓相同核心能力可透過不同 Recipe、Profile、Contract 或 Kit，套用到不同客戶、站點、資料來源與部署環境，而不必修改核心程式碼。

完成通用化至少必須符合：

1. 核心函式低耦合，具明確輸入、輸出與錯誤語意。
2. 專有名詞、顯示文字與客戶名稱不參與程式判斷。
3. 可變欄位、選項、單位、門檻、服務與路徑由設定或 Profile 提供。
4. 來源欄位透過 semantic mapping 對應穩定的 canonical key，不綁死特定欄名。
5. Kit 必須公開可被 Composer 或其他 Kit 消費的 capability contract；不以「檔案數」或「至少一支 HTTP API」作為唯一判準。
6. Kit 是可獨立選配、解析依賴、組裝、驗證，並提供完整業務價值的能力單位；單一 helper、model、hook 或純函式通常不單獨成 Kit。
7. 至少使用第二份 Profile，且該 Profile 與第一份相比，需在下列至少兩項有實質差異：欄位集合、驗證規則、workflow 分支、門檻或演算法參數；僅更換顯示文字或客戶名稱不視為顯著不同。須在不修改核心程式碼的情況下通過相同契約與驗收，才可宣告完成通用化。

通用化不得以犧牲資料正確性、安全性、可讀性或可測試性為代價，也不得為尚未出現的假設需求預建規則引擎、抽象框架或未使用的擴充點。

## 2. 規範優先順序

本文件適用於 Form System Kit Composer。規則依下列順序判定：

1. 使用者本次明確指示。
2. 已核准的 PRD、Schema、API、Kit Contract 與 Recipe。
3. 專案內的來源治理與測試規範。
4. 本文件的一般原則。
5. AI 或開發者的推測。

發現衝突時，依序以下列順序擇一裁決：

1. 有實際測試或產物驗證支持者優先。
2. 若支持程度相同，選較新核准者。
3. 若仍無法判斷，揭露衝突並交由使用者裁定，不自行假設。

並在交付說明中揭露衝突與處理方式。

## 3. AI 協作與 Data-First 流程

- 修改前先讀來源、呼叫端、資料模型及現有工具，不猜測高風險需求。
- 先確認 PRD、Schema、API、Manifest、Contract、Recipe，再定義成功條件、修改範圍及驗證方式。
- 採最小修改，不順手重構無關程式。
- 外部輸入、設定檔及 Kit Contract 必須在邊界驗證，錯誤需可行動、可追蹤。
- 未執行、跳過、環境受限或結果不確定的驗證必須分開揭露。
- 針對同一根因問題，連續三次修改後仍無法通過原驗證條件（而非改善但未通過，或轉為新的錯誤訊息），視為未收斂；應停止盲目試錯，整理已知事實、假設、失敗嘗試與下一個驗證點。
- 專案協作使用繁體中文且不使用 emoji。
- 延續既有預算要求：每任務 4,000 token、每 session 30,000 token；若不足以安全完成，應先縮小範圍或回報限制。

## 4. 通用化判斷順序

| 差異類型 | 應採用的機制 | 不應採用的方式 |
|---|---|---|
| 只有名稱或顯示文字不同 | terminology／i18n | 在條件式比對中文或客戶名稱 |
| 欄位、選項、單位、上下限不同 | schema／runtime profile | 在核心程式硬編碼欄名與門檻 |
| 流程、狀態、簽核條件不同 | workflow profile | 在主流程散落客戶專屬分支 |
| 可獨立啟用且具完整業務價值 | Kit／subfeature | 把每個 helper 或 model 都拆成 Kit |
| 演算法、協定或外部系統行為不同 | Adapter／extension Kit | 只增加一個無法表達行為差異的 JSON 參數 |

機器識別一律使用穩定 key；顯示文字使用可設定 label。禁止使用翻譯結果、中文顯示文字、客戶名稱或檔名作為核心控制邏輯。

## 5. 設定、Profile 與版本治理

設定必須分層：

- **Build-time Recipe**：選哪些 Kit／subfeature、組裝順序與依賴。
- **Runtime Profile**：欄位、選項、流程、門檻與可由授權角色調整的行為。
- **Environment Configuration**：連線字串、服務端點、檔案位置、硬體與部署環境。
- **Secret**：密碼、Token、私鑰與認證，只能由環境變數或秘密管理工具提供。

動態欄位必須宣告型別、stable key、label、預設值、選項、權限、驗證規則及 widget。Profile、表單與 Workflow 應版本化發布；歷史紀錄綁定建立時使用的版本，不得因新版本發布而改變既有資料語意。

設定中禁止任意執行 Python、JavaScript、SQL、Shell 或無限制 expression。若需要條件運算，應採有限、可驗證、可記錄的規則格式：規則格式不得包含迴圈、遞迴呼叫或自訂函式定義，僅允許固定運算子（比較、邏輯、算術）組成的條件式，且每條規則可獨立列出並個別測試。若需求超出此範圍，應視為需要程式碼變更，而非設定。

Vector Store、Knowledge Adapter 與回饋學習案例庫之索引與查詢，必須以客戶／站點為隔離邊界，不得跨客戶檢索或訓練，除非經雙方明確授權並記錄於 Recipe。

## 6. 核心演算法、可抽換參數與 Adapter

判斷原則：

- **核心演算法**決定「如何計算」，一般使用者不可任意修改。
- **可抽換參數**不改演算法本體，即可因客戶、站點、資料來源或部署環境而替換。
- **Adapter／流程模組**代表不同執行行為，必須實作統一介面，不能只塞進 JSON。

### 6.1 分析能力分界

| 分析階段 | 核心演算法／固定能力 | 可抽換參數 | 建議層級 |
|---|---|---|---|
| 資料清理 | 選欄、移除空列與全空欄 | 欄位清單、缺值策略、型別轉換 | Recipe／執行設定 |
| 資料切分 | 切分基準組與分析組 | target、threshold、分組數、時間區間、切分模式 | Workflow profile |
| 基本統計 | mean、median、標準差、CV、IQR、偏態、峰度 | 使用欄位、分位數、自由度、最小樣本數 | Capability options |
| 類別分析 | 類別與目標值的分布統計 | 類別欄位、目標欄位、normalize、未知類別策略 | Capability options |
| 單變量異常偵測 | IQR、3-Sigma、Min-Max | 方法、IQR multiplier、sigma 倍數、上下界 | Capability options |
| 相關性分析 | 相關矩陣與 baseline/current 差異 | Pearson／Spearman／Kendall、欄位、缺值策略 | Capability options |
| PCA | 標準化、主成分分解、loadings | component 數、解釋率門檻、scaler、random seed | Capability options |
| T²／SPE | Hotelling T²、SPE 與控制界線 | alpha、控制界線算法、baseline window | Capability options |
| 特徵重要性融合 | Delta correlation 與 PCA loading 融合 | alpha、貢獻門檻、正規化方法 | Capability options |
| 異常特徵排序 | Boundary／SPE／T² 分數合併 | 權重、top-k、同分規則 | Capability options |
| 拓樸分析 | 依拓樸追查可能來源 | 拓樸定義、節點、邊、站點映射、摘要深度 | 客戶資料／Adapter 設定 |
| RAG 檢索 | embedding、向量搜尋、證據組合 | provider、model、collection、top-k、相似度門檻 | External service options |
| NG Code 診斷 | 依知識庫對事件評分與預測 | 知識庫、信心門檻、observer、規則權重 | 客戶資料／Capability options |
| 相似案例搜尋 | 案例相似度與提示檢索 | top-k、最低分數、重疊門檻、機台限制 | Capability options |
| LLM 綜合分析 | 組合事件、案例與證據產生判斷 | endpoint、model、prompt、timeout、retry、temperature、認證 | External service options |
| 回饋學習 | 依人工確認結果分類與統計錯誤 | 儲存位置、確認欄位、索引策略、保留期限 | Governance options |

### 6.2 不得固定在核心的內容

| 綁定內容 | 通用化後 |
|---|---|
| `P1/P2/P3` | `stationProfiles[]` 或站點映射 |
| `UT` | `serializerAdapter`／客戶 Profile |
| `Machine`、`Measurement` | semantic role mapping |
| `P1.Machine_No.` 等來源欄位 | source-to-canonical field mapping |
| `daily`／`complain` | workflow profile |
| 固定輸出目錄或設定檔路徑 | CLI、Environment Configuration 或 Recipe option |
| LLM endpoint、ChromaDB 路徑 | `externalServices` |
| RAG／NG 知識庫檔案 | 客戶資料資產宣告 |
| `top_n`、`min_score`、`alpha`、`threshold` | 對應 capability options |
| 固定相關性方法 | correlation method option |

### 6.3 必須做成 Adapter 的行為

| Adapter | 統一介面責任 |
|---|---|
| Input Adapter | 將 CSV、JSON、資料庫或 API 輸入轉為 canonical dataset |
| Schema Adapter | 將客戶欄位映射到 id、target、station、machine、measurement 等語意角色 |
| Serializer Adapter | 將分析結果轉成版本化標準事件合約 |
| Vector Store Adapter | 建立、載入、查詢向量索引 |
| Embedding Adapter | 將知識內容轉為 embedding |
| LLM Adapter | 統一認證、timeout、retry、錯誤與回應格式 |
| Knowledge Adapter | 載入不同客戶的 SOP、NG code 與歷史案例 |
| Output Adapter | 寫入檔案、資料庫或事件系統，並回報副作用結果 |

Input Adapter 僅負責格式解析與型別還原（如 CSV→表格），不得進行語意映射；欄位到語意角色（id、target、station 等）的映射一律歸屬 Schema Adapter，兩者責任不得重疊。

送往外部 LLM 或 embedding 服務前，須依欄位分類標記執行 PII／機敏欄位遮罩或排除；LLM Adapter 須記錄實際送出欄位清單以供稽核。

## 7. Kit 設計與契約

Kit 應對應完整、可辨識的業務能力，並宣告：

- `id`、`displayName`、`category`、`businessCapability`
- `provides`、`consumes`
- required／optional dependencies 與版本限制
- backend services、router、model、migration、registry entry
- subfeatures、permissions、feature flags
- external services、環境需求與降級策略
- source ownership、source paths 與 package sources
- capability 的 input、output、errors、auth、side effects

Kit 間只透過 Contract、API 或 capability 溝通，不得直接引用其他 Kit 的內部檔案、私有函式或私有資料表；此原則須以 CI 靜態檢查（如 import 邊界檢查）強制執行，人工 review 不視為充分保證。Manifest、Contract、Recipe、resolved plan、registry 與 database plan 必須一致。

Kit Contract 之破壞性變更須遞增主版本號，並保留至少一個過渡版本供舊 Recipe 相容；棄用中的 Contract 版本須標示 deprecation 日期與遷移指引。

對分析系統，建議先維持單一 `analytics-kit`，再以 subfeature 選配：

```text
analytics-kit
├─ descriptive-statistics
├─ categorical-analysis
├─ outlier-detection
├─ correlation-analysis
├─ pca-diagnostics
├─ feature-ranking
├─ topology-analysis
├─ rag-retrieval
├─ ng-code-diagnosis
├─ similar-case-search
├─ llm-synthesis
└─ feedback-indexing
```

演算法實作放在 Kit source；門檻、方法與模型放在 capability options；UT、P1/P2/P3 與客戶欄位放在 Adapter／Profile；外部服務放在 `externalServices`；輸入、輸出、錯誤與副作用放在版本化 Contract。

## 8. 來源與生成產物

- `kits/`、`templates/`、`tools/` 等來源是修改入口。
- `generated/`、`dist/` 為衍生產物，不得手動修改。
- 已有來源碼時優先抽取；只有來源不存在的標準能力才建立 template。
- 修改唯一來源後，使用 assembly／package 流程重新生成並驗證產物。
- 不採「同步手改所有副本」；來源與產物不一致時應修正生成流程或來源宣告。

## 9. 先前 Analytical-Four 審查的通用化缺口

以下為 2026-08-11 先前唯讀審查的紀錄性結論；它描述當時狀態，實作後需重新驗證，不得直接當成永久事實。本節結論須於對應實作項目**開始前**由負責人重新確認前提仍成立，並記錄確認日期；逾三十天未重新確認者，實作前必須重新審查，不得逕行套用。

### P0：先形成可重現、可描述的最小 Kit

1. **可重現執行包**：提供 example config、最小輸入 fixture、知識庫／向量庫建立範例、預期輸出或 golden sample，並調整 `.gitignore`，允許追蹤 Manifest、Schema、Recipe 與 fixtures。
2. **Repository-wide 可驗證性**：修復所有追蹤中 Python 檔案的語法問題，確保靜態檢查與封裝不因非主流程檔案失敗。
3. **Kit Manifest**：建立 `analytics-kit` 的業務能力、依賴、subfeatures、permissions、external services、feature flags 與 source ownership 宣告。
4. **Capability I/O Contract**：正式定義 input、output、errors、auth、side effects 與 schema version；Python dataclass 不能取代跨 Kit 契約。

### P1：解除核心業務綁定

1. 以 semantic mapping 與 Adapter 移除 UT、P1/P2/P3、Machine、Measurement 及固定 serializer 欄位的核心綁定。
2. 建立 stage registry、dependency graph、enable／disable options、graceful degradation 與 resolved plan，讓 Recipe 可控制分析階段。
3. 在資料載入、設定載入、輸出、LLM 與向量庫邊界補齊成功／失敗語意、timeout、retry、HTTP 狀態、認證及事件隔離。
4. 以 `externalServices` 宣告 LLM、embedding、vector store、RAG knowledge、case storage、離線模式與 fallback。

### P2：達成可組裝與可交付

1. 補齊 Recipe Schema、validation、dependency resolver、resolved plan、Assembly IR、package source list 與 generated registry。
2. 建立演算法 unit tests、Adapter／Serializer contract tests、pipeline integration tests、邊界資料測試、外部服務失敗測試、deterministic fixtures 與 golden JSON。
3. 定義 CLI、環境變數、install／start／health-check、輸入輸出目錄、容器或套件化、CPU／GPU fallback、模型與知識庫版本、job status／progress／cancel／retry。
4. 提供 Composer Kit Catalog 所需的業務說明、選配問題、preview data、preview scenarios、mock responses、I/O 範例及外部需求提示。

先前審查估計當時約完成 30%，最大缺口不是演算法數量，而是「演算法之外的產品契約、資料映射與組裝邊界」。

## 10. 建議導入順序

1. 建立可重現的 example config、fixtures、output schema 與最小 smoke test。
2. 定義 `analytics-kit` Manifest 與 capability I/O contracts。
3. 將 UT、P1/P2/P3 與來源欄位抽成 semantic mapping、Adapter 與 Profile。
4. 建立 stage registry，讓 Recipe 可選配能力並解析依賴。
5. 補齊 external service 降級策略、錯誤邊界、測試與部署合約。
6. 最後接 Composer GUI preview，並執行 generated package 的完整驗收。

每一階段都必須驗證「有安裝」與「未安裝／不可用」的組合，避免 optional capability 變成隱性必需依賴。

## 11. 測試與完成條件

- 測試描述行為存在的原因，而不只追求覆蓋率。
- 每次修改提供實際執行的命令、exit code 與結果摘要。
- Kit 變更至少驗證 Recipe、Resolver、Contract、Registry、Database Plan、Generated System 與 Package Folder。
- E2E 使用隔離資料庫與獨立 port，並清理暫存產物。
- 通用化驗收至少包含：符合第1節第7點定義之兩份顯著不同 Profile、欄位名稱替換、optional service 不可用、缺欄位、空資料、小樣本、NaN／inf、timeout、非預期回應與部分成功。
- 完成回報採「摘要、修改檔案、Passed、未驗證／受限項目」格式。

## 12. Mailbox 協作

- 保留 Codex 實作、Claude review／tests、使用者 requirements 的角色分工。
- 僅在使用者要求 Mailbox 或既定協作流程適用時建立 thread。
- Thread 使用 YAML frontmatter、typed artifacts、Decisions 與 Open Questions。
- Claude 建議先分類為 Accepted、Rejected、Deferred、User-owned，不直接自動修改。
- 已解決問題以刪除線與決策結果保留歷史；實作、驗證與尚未處理事項需分開記錄。

## 13. 禁止過度通用化

下列情況應維持簡單實作，直到有第二個真實案例：

- 只有單一實作卻建立 factory、interface 或 plugin framework。
- 為假設中的未來客戶加入未使用欄位或擴充點。
- 把純 helper、model、hook 或單一演算法強行拆成獨立 Kit。
- 用通用規則引擎取代數個清楚、穩定且可測試的條件式。
- 為了「可設定」而把安全、資料一致性或演算法不變量交給一般使用者修改。

本節所稱「過度通用化」，指的是為尚無需求的**行為差異**預先建立抽象；Kit Manifest、Contract 邊界與 Adapter 介面屬於**契約基礎設施**，即使僅有單一客戶亦應建立，不受本節限制。

原則是：先以第二個真實 Profile 證明差異，再抽取最小、可驗證的共通邊界。

## 14. 資訊安全與軟體保護

### 14.1 信任邊界與適用範圍

本節規則涵蓋兩個性質不同的信任邊界，須分開適用，不得混用同一套控制假設：

- **邊界一：Composer 平台（多租戶 SaaS）**：使用者以帳號登入、於雲端環境組裝 Recipe、選配 Kit、管理 Profile 之過程。此邊界的資料（客戶的 Recipe、自訂 Kit 原始碼、fixture、業務邏輯）全程在供應方控制範圍內，威脅模型以多租戶隔離、平台帳號與 API 安全為主。
- **邊界二：交付物（assembled system）**：使用者於 Composer 上完成組裝並匯出／部署後產生的系統，不論落地於客戶本地機房或客戶自有雲端帳號，自匯出當下起即脫離供應方直接控制，威脅模型以防竄改、防未授權複製與逆向、邊界輸入驗證為主。

適用原則：

1. 14.4～14.11（防未授權複製、供應鏈完整性、輸入驗證、各類注入防護、Recipe 動態載入安全）以**交付物**為主要適用對象；交付物若本身仍為多租戶系統（例如客戶將其部署於自有雲端帳號並提供多廠區／多客戶使用），須將租戶隔離能力內建於 Kit Contract（如 Schema Adapter、Output Adapter 支援 tenant scoping），使同一套隔離邏輯可同時保護兩個邊界，不得只在 Composer 平台實作。
2. 14.12（LLM／RAG）、14.13（認證授權）、14.14（安全日誌）同時適用於 Composer 平台與交付物。
3. Composer 平台本身另受 14.3「Composer 平台租戶隔離」規範；該節為現在進行式風險，優先順序不得因交付物相關工作而延後。
4. 匯出／部署動作是邊界一與邊界二的交界點，須驗證使用者對來源 Kit、Profile 與客戶資料具合法存取權，並記錄匯出事件（見 14.3）。

### 14.2 安全目標與限制

系統至少保護下列屬性：

- **機密性**：原始碼、私鑰、Token、客戶資料與內部設定不得被未授權取得。
- **完整性**：程式、Kit、Recipe、Profile、migration、模型與更新包不得被未授權竄改。
- **可用性**：攻擊、資料異常或外部服務失效時，核心能力能拒絕危險操作或安全降級。
- **可追溯性**：能識別建置來源、版本、發布者、授權對象及異常操作。
- **不可否認性**：正式產物可驗證由合法發布者簽署。

交付到客戶控制環境的程式無法保證完全不可複製或逆向；安全目標應是限制未授權使用、提高複製成本、保護真正機密、偵測竄改並留下可追蹤證據。混淆、壓縮、隱藏檔案、前端限制與非公開格式只能作為輔助措施，不得視為安全邊界。

安全設計應採多層防禦，並遵循 [NIST Secure Software Development Framework](https://csrc.nist.gov/projects/ssdf) 的四類實務：準備組織、保護軟體、產出安全軟體及回應漏洞。

### 14.3 Composer 平台租戶隔離

Composer 平台為多租戶 SaaS：不同客戶帳號透過同一平台組裝系統，平台資料庫、物件儲存與快取中同時存有多個客戶的 Recipe、Profile、自訂 Kit 原始碼與 fixture，其保護要求不得低於交付物本身：

1. 所有 Composer 平台資料查詢與寫入（Recipe、Profile、自訂 Kit 來源、fixture、匯出紀錄）須強制帶入可信 tenant context，不得以請求本文、前端傳入的 tenant／customer ID 作為唯一依據；後端須獨立核發並驗證該 context。
2. 客戶上傳或引用外部 Kit 來源、Kit registry URL 作為 Recipe 依賴時，須套用 SSRF 防護（allow-list 網域、禁止內網位址與 cloud metadata endpoint、限制 scheme、設定 timeout 與回應大小上限）。
3. 平台資料庫與物件儲存須加密；視風險評估決定是否採 tenant 級獨立加密金鑰，以降低資料庫層級單點外洩造成全租戶資料同時洩漏的風險。
4. 使用者僅能檢視、編輯、匯出、部署其所屬租戶擁有或已獲授權共用的 Recipe、Profile 與 Kit；跨租戶存取須回傳拒絕並記錄安全事件（呼應 14.14 安全日誌與 14.15 驗收條件）。
5. 匯出／部署為交付物之動作，須記錄操作者、租戶、匯出內容（Recipe、Kit 版本、Profile）與目的地（本地／客戶雲端），作為稽核與外洩追查依據。
6. Composer 平台前端之登入 session、狀態變更操作（建立、修改、刪除、匯出）須於伺服器端執行 authorization，並對 cookie-based session 採用 CSRF token 或 SameSite cookie 屬性防護，不得僅以前端顯示邏輯限制操作。

### 14.4 防止未授權複製與使用

1. 正式交付包採最小交付原則，不得包含：

   - 非執行必要的原始碼、source map、測試工具與 debug endpoint。
   - 私鑰、簽章金鑰、測試帳號、`.env` 或內部部署文件。
   - 未選用的 Kit、客戶資料、fixture 或開發暫存產物。

2. 若產品需要授權控制，授權檔必須使用非對稱式數位簽章，至少包含：

   - `licenseId`、`customerId`／`tenantId`、`productId`。
   - 可使用的 Kit／feature、授權版本與 `schemaVersion`。
   - 生效、到期及簽發時間。
   - 裝置綁定資訊；只有商業需求明確且硬體識別可靠時才啟用。
   - `issuer`、簽章演算法與 `signature`。

3. 私鑰只能存在於受控簽章環境，不得寫入 repository、容器 image 或客戶端；客戶端只能持有公開金鑰。
4. 授權、tenant、role 與 feature entitlement 必須由可信後端逐次驗證，不得依賴前端隱藏按鈕、客戶端旗標或可修改的 Profile。
5. 可為每個客戶交付包加入不影響功能的 build／customer fingerprint，以協助追查外洩來源；不得將其偽裝成安全驗證本身。
6. 高價值演算法若不可接受被客戶取得，應部署在供應方控制的 API、服務或受信任硬體內；bytecode、minification、binary packaging 或 obfuscation 只能增加逆向成本。

### 14.5 防止程式、設定與發布包遭竄改

每次正式發布應產生並保存：

- Artifact hash manifest。
- 發布包數位簽章與簽章憑證識別。
- 版本、來源 commit、建置時間與建置流程版本。
- Software Bill of Materials（SBOM）。
- Build provenance／來源證明。

安裝、啟動與更新前至少驗證：

1. 發布包簽章有效且簽發者受信任。
2. 關鍵檔案 hash 與 Manifest 一致。
3. Kit Manifest、Contract、migration、registry 與實際產物一致。
4. 更新版本符合允許的升級路徑，防止未授權 downgrade／rollback。
5. 授權檔、Recipe、Profile 或外掛來源符合其信任政策。

驗證失敗時依風險分級：

- 核心程式、migration、registry、授權或安全設定：拒絕啟動、安裝或更新。
- 可選 Profile、預覽資產或 optional capability：隔離該資產並安全降級。
- 所有失敗都寫入結構化安全事件，包含版本、檔案、預期值、實際值與 correlation ID；Log 不得包含 Secret 或完整敏感資料。

正式產物只能由受控 CI/CD 從核准來源建立。版本庫及建置環境至少啟用 MFA、最小權限、protected branch、必要 code review、Secret scanning 與稽核紀錄。依賴必須鎖定版本、產生 SBOM、掃描已知漏洞並防範 dependency confusion。相關供應鏈控制依 [OWASP Software Supply Chain Security Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Software_Supply_Chain_Security_Cheat_Sheet.html) 執行。

### 14.6 不可信輸入與共同邊界

所有跨邊界資料均視為不可信，包括：

- HTTP request、webhook、CLI 參數與環境變數。
- CSV、Excel、JSON、ZIP、檔名與檔案路徑。
- Recipe、Profile、Manifest、表單定義與資料庫既有內容。
- 第三方 API、RAG 文件、向量檢索結果與 LLM 回應。

每個邊界必須定義 Schema、型別、長度、數量、格式、允許值、權限及錯誤處理。優先採封閉式 allow-list 與安全、參數化 API；輸入驗證是必要補強，但不能取代安全 API。通用注入防護依 [OWASP Injection Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/Injection_Prevention_Cheat_Sheet.html) 執行。

### 14.7 SQL 與查詢注入

- SQL 一律使用 parameterized query、prepared statement 或 ORM bind parameters。
- 禁止以字串串接、format string、template 或使用者輸入產生 SQL 結構。
- 資料表名、欄位名、聚合方式及排序方向無法參數化時，必須映射到程式內固定 allow-list；不得直接使用外部文字。
- Stored procedure 內不得再組合不安全的 dynamic SQL。
- 應用程式資料庫帳號採最小權限，讀取、寫入、migration 與管理用途分離。
- 不得只依賴 escaping；實作原則依 [OWASP SQL Injection Prevention Cheat Sheet](https://cheatsheetseries.owasp.org/cheatsheets/SQL_Injection_Prevention_Cheat_Sheet.html)。

### 14.8 OS Command Injection

- 優先使用標準函式庫或專用 API，不呼叫 Shell。
- 禁止把外部輸入組成 PowerShell、Shell 或批次命令字串。
- 無法避免外部程序時，必須使用固定 executable、argument array、allow-list、低權限帳號、固定工作目錄及 timeout。
- 不接受外部輸入指定 executable、script path、shell operator 或任意環境變數。
- 驗證參數長度、格式與允許值；錯誤輸出不得直接回傳內部路徑、命令或 Secret。

### 14.9 XSS、HTML 與 Template Injection

- 前端不得直接渲染未清理的 HTML；優先使用框架預設 escaping。
- `dangerouslySetInnerHTML` 或等價功能必須經安全審查並使用經核准的 sanitizer。
- 富文字、SVG、Markdown 及外部 URL 必須依輸出情境清理。
- Template 名稱與檔案來源只能從受信任 registry 或 allow-list 選取。
- 正式環境應設定適當 Content Security Policy，限制 script、frame、object 與外部資源來源。

### 14.10 Path Traversal、上傳與壓縮檔攻擊

- 上傳檔名不得直接作為實際儲存路徑，應產生伺服器端識別碼並分離原始顯示名稱。
- 路徑 canonicalize 後必須確認仍位於明確允許的根目錄內。
- 拒絕 `..`、非預期絕對路徑、UNC／裝置路徑、符號連結逃逸與 alternate data stream。
- ZIP 或其他封裝檔解壓前逐一驗證 entry，防止 Zip Slip。
- 限制單檔大小、檔案數量、總上傳量、解壓後總容量及壓縮比例。
- 不只相信副檔名或 `Content-Type`；應檢查實際格式，並在隔離、低權限環境解析高風險檔案。

### 14.11 Recipe、Profile 與動態載入安全

- Recipe、Profile、Manifest 必須通過封閉式 JSON Schema 或等價驗證；安全敏感文件應拒絕未知欄位。
- 禁止設定執行任意 Python、JavaScript、SQL、Shell、模板或無限制 expression。
- `module`、`router`、`migration`、`sourcePath`、Adapter 與外部服務類型只能引用受信任 registry。
- 不得允許使用者指定任意 import path、本機程式檔、URL scheme 或未簽署外掛。
- Recipe 解析後必須再次驗證 resolved plan，不得假設通過 Schema 即代表依賴、權限及來源皆安全。
- Kit 能否被載入需同時通過簽章、版本、entitlement、dependency、permission 與 source ownership 驗證。

### 14.12 LLM 與 RAG Prompt Injection

- RAG 文件、使用者提示、工具結果與外部 API 回應一律視為不可信資料，不得提升為 system instruction。
- System prompt、工具權限、tenant context 與客戶資料必須分層隔離。
- 模型不得直接執行 SQL、Shell、檔案修改、部署、權限變更或外部傳輸。
- 工具呼叫採固定 allow-list、嚴格參數 Schema、伺服器端 authorization 與最小權限。
- 模型輸出必須通過 JSON Schema、業務規則及 tenant 檢查後才能進入後續流程。
- 寫入資料、修改設定、執行程式或傳送外部資料前，依風險要求額外授權或人工確認。
- Secret、私鑰、完整連線字串及跨租戶資料不得放入 prompt、trace 或向量庫。
- 知識文件需記錄來源、版本、tenant、ACL 與 integrity hash；所有檢索強制套用 tenant filter，不能由模型選擇是否過濾。

### 14.13 認證、授權與執行環境

- 認證與授權分離：成功登入不代表可使用所有 Kit、API、資料或管理操作。
- API、背景任務、WebSocket、匯出、下載與管理操作都必須在伺服器端執行 authorization。
- 所有 tenant-scoped 資料查詢及寫入都必須強制帶入可信 tenant context，不採用請求本文中的 tenant ID 作為唯一依據。
- 管理、建置、簽章、runtime 與資料庫帳號分離，權限遵循 least privilege。
- 容器或服務盡可能使用非 root 帳號、唯讀檔案系統、受限網路與最小 Linux capabilities／Windows 權限。
- Secret 由環境或秘密管理工具注入，支援輪替；不得寫入 Log、錯誤回應、測試 fixture 或前端 bundle。

### 14.14 安全日誌與事件回應

至少記錄：

- 登入失敗、權限拒絕、跨租戶嘗試與高風險管理操作。
- 授權、簽章、hash、來源或版本驗證失敗。
- Recipe／Manifest 驗證失敗與未授權 Kit 載入。
- 檔案上傳拒絕、路徑逃逸、注入特徵、異常工具呼叫與 rate limit。
- 依賴或映像漏洞、Secret 外洩及撤銷事件。

安全日誌需具時間、actor、tenant、action、resource、result 與 correlation ID，並防止一般使用者修改。必須定義漏洞通報、分級、修補、撤銷授權／簽章憑證、發布安全更新及通知受影響客戶的流程。

### 14.15 安全驗收條件

| 類別 | 最低驗收案例 | 期望結果 |
|---|---|---|
| 授權 | 偽造簽章、過期、錯誤客戶／裝置、未授權 feature | 後端拒絕並留下不含 Secret 的安全事件 |
| 完整性 | 修改 Python／JS、替換 Manifest、修改 migration、刪除關鍵檔案 | 安裝、啟動或更新失敗；不可靜默繼續 |
| SQL 注入 | 引號、布林條件、註解、union、惡意排序欄位 | 輸入被當成資料或被 allow-list 拒絕 |
| Command 注入 | `;`、pipe、重導向、命令替換、PowerShell metacharacters | 不經 Shell 解譯，危險參數被拒絕 |
| 路徑與壓縮檔 | `../`、絕對路徑、UNC、符號連結、Zip Slip、壓縮炸彈 | 不寫出允許根目錄，超限檔案被隔離 |
| XSS／模板 | script、事件屬性、惡意 SVG、URL scheme、模板名稱 | 內容被安全編碼、清理或拒絕 |
| 設定注入 | 任意 module、source path、expression、未知欄位 | Schema／registry 驗證失敗且不載入 |
| LLM／RAG | 文件內越權指令、工具誘導、跨租戶資料要求 | 指令不升權，工具拒絕，資料不外洩 |
| 供應鏈 | 未簽章套件、錯誤 hash、未知依賴、來源不明建置 | 發布或部署 gate 失敗 |
| 權限 | 一般使用者呼叫管理 API、跨 tenant 存取、修改 feature flag | 伺服器端拒絕並記錄事件 |
| Composer 平台租戶隔離 | 以租戶 A 憑證嘗試存取／匯出租戶 B 之 Recipe、Profile、自訂 Kit 來源 | 伺服器端回傳拒絕（403／404），不洩漏是否存在，並記錄安全事件 |
| SSRF | Recipe／Kit 來源 URL 指向內網位址、cloud metadata endpoint 或非 http(s) scheme | 請求被 allow-list 拒絕，不發出對外連線 |
| CSRF | 未帶 CSRF token 或跨站來源之狀態變更請求（建立、修改、刪除、匯出） | 請求被拒絕並記錄事件 |

安全驗收至少包括 SAST、dependency／container scanning、Secret scanning、Contract／Schema negative tests，以及針對實際部署入口的 DAST 或等價動態測試。掃描結果需人工判讀；通過工具掃描不代表不存在漏洞。

不得以「前端已限制」「一般使用者不會這樣操作」「程式已混淆」或「目前沒有公開 URL」作為接受安全缺口的理由。所有安全例外都必須記錄風險、影響範圍、補償控制、責任人及到期日。