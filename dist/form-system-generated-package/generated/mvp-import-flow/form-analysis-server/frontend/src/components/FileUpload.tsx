export function FileUpload() {
  // Deprecated component: legacy CSV upload pipeline has been removed.
  // Keep this module as a visible placeholder in case old routes still import it.
  return (
    <div className="p-4 rounded border border-amber-200 bg-amber-50 text-amber-900 text-sm">
      此元件已停用（legacy CSV 上傳流程已移除）。請改用 UploadPage 的 v2 import jobs 流程。
    </div>
  );
}