#!/usr/bin/env bash
set -e
echo "generic-forms-kit: schema-driven form types and generic record storage enabled."
echo "  Backend: /api/forms (CRUD), /api/forms/{code}/schema, /api/forms/{code}/records, /api/forms/{code}/upload"
echo "  Frontend: Generic Forms tab for managing form schemas and viewing records."
